
import React from 'react';

const VaultCreditDisplay: React.FC = () => (
  <div className="p-4 bg-yellow-800 text-white">
    Vault Credit Display — Shows accumulated Forum currency and streaks.
  </div>
);

export default VaultCreditDisplay;
